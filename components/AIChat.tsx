
import React from 'react';

// Feature Disabled: Separate Assistant Tab removed as per user request.
// AI features are now integrated directly into modules via AIAdvisor.
export const AIChat: React.FC = () => {
  return null;
};
